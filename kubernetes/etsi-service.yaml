apiVersion: v1
kind: Service
metadata:
  name: taxwizard-app-service
  namespace: etsi-taxwizard-app
  labels:
    app: taxwizard-app
spec:
  type: LoadBalancer
  selector:    
    app: taxwizard-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80